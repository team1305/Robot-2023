package frc.robot.commands.auto.SafeCommands;

import frc.robot.utils.SafeCommand;

public class EndCommands {
    public static SafeCommand getSafeCommand(

    )
    {
        return new SafeCommand(null, true);
    }
}
